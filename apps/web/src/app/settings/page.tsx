import { PlaceholderScreen } from "@/components/product/placeholder-screen";

export default function SettingsPage() {
  return (
    <PlaceholderScreen
      badge="Ajustes"
      title="Los ajustes del producto aun no estan implementados."
      description="Se deja una ruta temporal para mantener la navegacion coherente con la especificacion."
      primaryHref="/home"
      primaryLabel="Volver al home"
    />
  );
}
